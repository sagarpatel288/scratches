
// Defining a variable
var variable = "variable"
val immutable = "immutable"

// Accessing the variable
variable.uppercase()
immutable.plus(" value")

// Defining a function
val funName = { a: Int, b: Int -> a + b }

// Accessing the function
funName.invoke(1, 2)

